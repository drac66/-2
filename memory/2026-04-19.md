# 2026-04-19

- Helped user connect local Spring Boot backend (`project/backend`) to Supabase Postgres.
- Confirmed backend datasource config uses env vars in `application.yml` (`DB_URL`, `DB_USER`, `DB_PASSWORD`).
- Found local Maven available at `C:\Dev\apache-maven-3.9.15\bin\mvn.cmd` (not in PATH initially).
- Diagnosed repeated DB failures as network-layer connectivity issues (direct DB host resolved to IPv6; `5432` timeout). Also observed `pooler:6543` blocked on some networks.
- Switched to Supabase pooler connection approach (using `DATABASE_URL`/pooler host and port `6543`) instead of direct DB host.
- Verified success after user enabled workable VPN/network path: pooler TCP reachable, backend on `127.0.0.1:8080` running, `GET /api/messages` returned success.
- End-to-end API smoke tests passed for:
  - `/api/messages` (POST + GET)
  - `/api/diary` (POST + GET)
  - `/api/albums` (POST + GET)
- Frontend check: `project/frontend/app.js` already points to `apiBase: 'http://127.0.0.1:8080'`.
- User manually validated message POST/GET in PowerShell; Chinese text display issue identified as terminal encoding only (not DB corruption).
- Cleaned smoke-test records from DB (messages/diary/albums test rows removed) to keep data tidy.
- User progressed to WeChat DevTools with real AppID; latest issue shown is simulator startup failure due JSON type mismatch in `pages/login/login.json` (`Expecting STRING ... got TRUE`), likely a malformed JSON field value needing correction.
