# 2026-04-25

- User is actively developing `wxcloud-version/miniprogram` and wants to continue focusing on mini program features.
- Confirmed UI/UX requests implemented during session (as tracked in conversation):
  - Album images: tap to full-screen preview with zoom; long-press save behavior requested both on list image and in full-screen preview.
  - Messages and Diary pages: show nickname/author identity again, but as a smaller line at the bottom for readability.
- Major bug context captured from user screenshot/report:
  - AI page had no practical send shortcut (Enter/confirm not triggering send).
  - `aiChat` cloud function failed at runtime with `Cannot find module 'wx-server-sdk'` (dependency/deploy issue), causing callFunction failures.
- Product direction decision from user:
  - Wants in-mini-program AI assistant for both partners with separate personal chat windows (isolated per user), but same backend AI service.
  - Intended use on phone/tablet for light Word/PDF handling (not full “pro” IDE-level file editing).
  - Needs file receive/edit/send workflow in-app.
- Infrastructure detail provided by user:
  - Relay/base site is `https://cmdme.cn`.
  - User shared an API key in chat; treat as exposed/compromised and rotate immediately before production use.
- Deployment/security constraints to remember for this project:
  - Keep API keys server-side only (cloud function env vars), never in mini program frontend.
  - For cloud functions, ensure dependencies are installed/deployed in cloud environment ("upload and deploy: install dependencies").
